---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: ipamallocations.controller.kubeslice.io
spec:
  group: controller.kubeslice.io
  names:
    kind: IPAMAllocation
    listKind: IPAMAllocationList
    plural: ipamallocations
    singular: ipamallocation
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: IPAMAllocation is the Schema for the ipamallocation API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: IPAMAllocationSpec defines the desired state of IPAMAllocation
            properties:
              clusterAllocations:
                description: ClusterAllocations tracks IP allocations per cluster
                items:
                  description: ClusterIPAllocation defines IP allocation for a specific
                    cluster
                  properties:
                    allocatedAt:
                      description: AllocatedAt is the timestamp when the allocation
                        was made
                      format: date-time
                      type: string
                    clusterName:
                      description: ClusterName is the name of the cluster
                      type: string
                    status:
                      description: Status indicates the current status of the allocation
                      enum:
                      - Allocated
                      - Released
                      - Pending
                      type: string
                    subnetCIDR:
                      description: SubnetCIDR is the allocated subnet for this cluster
                        (e.g., 10.1.1.0/24)
                      type: string
                  required:
                  - allocatedAt
                  - clusterName
                  - status
                  - subnetCIDR
                  type: object
                type: array
              sliceName:
                description: SliceName is the name of the slice for which IP allocation
                  is being tracked
                type: string
              sliceSubnet:
                description: SliceSubnet is the base subnet CIDR for the slice (e.g.,
                  10.1.0.0/16)
                type: string
            required:
            - sliceName
            - sliceSubnet
            type: object
          status:
            description: IPAMAllocationStatus defines the observed state of IPAMAllocation
            properties:
              allocatedSubnets:
                description: AllocatedSubnets tracks the number of currently allocated
                  subnets
                type: integer
              availableSubnets:
                description: AvailableSubnets tracks the number of available subnets
                  in the slice
                type: integer
              lastUpdated:
                description: LastUpdated is the timestamp of the last status update
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
